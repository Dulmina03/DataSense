<Window x:Class="DataSense.UI.NetSpeedMeterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:DataSense.UI.Converters"
        Title="NetSpeedMeter"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="Transparent" BorderThickness="0" Padding="6,4">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- Download Speed -->
            <StackPanel Orientation="Horizontal" Margin="0,1">
                <TextBlock Text="▼" Foreground="#4FC3F7" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0" FontWeight="Bold"/>
                <TextBlock x:Name="DownloadText"
                           Text="0 B/s"
                           FontFamily="Segoe UI"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           MinWidth="70"
                           FontSize="{Binding SelectedFontSize}"
                           Foreground="{Binding TextBrush}"/>
            </StackPanel>

            <!-- Upload Speed -->
            <StackPanel Orientation="Horizontal" Margin="0,1">
                <TextBlock Text="▲" Foreground="#EF5350" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0" FontWeight="Bold"/>
                <TextBlock x:Name="UploadText"
                           Text="0 B/s"
                           FontFamily="Segoe UI"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           MinWidth="70"
                           FontSize="{Binding SelectedFontSize}"
                           Foreground="{Binding TextBrush}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
